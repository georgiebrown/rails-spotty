<div class="container">
<div class="d-flex justify-content-between">
<%= link_to spot.name, spot_path(spot)%><div><%= link_to '<i class="fas fa-heart"></i>'.html_safe, spot_favourites_path(spot), method: :post%></p>
</div>
</div>
<p><%= spot.location %></p>
<p><%= spot.category.name %></p>
<% spot.stories.each do |story| %>
<p><%= spot.stories.first.user.first_name %> went here:
<%=story.content %></p>
<% end %>
<% if spot.photos.first %>
<%=  cl_image_tag spot.photos.first.file.key, height: 350, width: 350, crop: :fill, class: "card-img-top"%>
<% end %>
</div>

