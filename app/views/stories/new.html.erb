<div class="container">
  <div class="row justify-content-center">
    <div class="col-sm mt-5">
      <%= simple_form_for [@spot, @story] do |f| %>
      <%= f.input :photo, as: :file %>
      <%= f.input :content %>
      <%= f.button :submit, class: "btn btn-dark" %>
      <% end %>
    </div>
  </div>
</div>
